# src/components — cross-feature UI primitives

Only genuinely shared primitives live here (button, card, badge, modal, nav shell, ...) — built
in M1 (design system). Feature-specific UI belongs inside its slice, not here.
