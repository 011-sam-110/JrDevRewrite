# Junior Dev — Build Roadmap

Driven by the `/goal` skill: one milestone per run, test-first, gated, committed, then stop.
Scope authority: **CLAUDE.md** (binding spec, 2026-06-10) + `junior-dev-prd.md` v0.3 (§12 decisions).
Statuses: `[ ]` todo · `[~]` in progress · `[x] done (date, commit)` · `[!] blocked: reason`.

Ordering logic: foundation → design system → identity → the prize-pool loop end-to-end (the core
product thesis) → battles (the second mode, with its own infra) → AI generation → deploy → marketing.
Each milestone is sized to be one reviewable checkpoint.

---

## Phase A — Foundation

### M0: Scaffold & toolchain
- Status: `[x] done (2026-06-10, 84cabeb)` — Next 15.5 + TS 5.9 strict, Drizzle/Postgres 17, Vitest + Playwright, hooks live
- Depends on: —
- Spec: CLAUDE.md → Stack, Commands, Architecture
- Acceptance:
  - [x] Own git repo initialized in this folder (nested inside the home repo is expected — always run git from inside the project, same gotcha as EV_Scraper), `core.hooksPath .githooks` set, initial commit.
  - [x] Next.js 15 App Router, TypeScript strict; eslint + prettier configured.
  - [x] VSA skeleton folders (`src/features|domain|infra/db|app/components|lib`, `content/pools/`, `tests/e2e/`) with placeholder barrel/readme files so the structure is real.
  - [x] Drizzle + local Postgres via `docker compose` (db service), `db:generate`/`db:migrate` scripts working with a trivial first table.
  - [x] Vitest running (one passing kernel placeholder test), Playwright installed with one passing smoke e2e (home page renders).
  - [x] Quality-gate hooks + pre-commit gate verified live (they no-op'd pre-scaffold).
  - [x] `npm run dev` serves http://localhost:3000; typecheck/lint/test all green.

### M1: Design system
- Status: `[x] done (2026-06-10, b6951b0)` — "arena terminal" tokens (volt/gold/elo on OLED blue-black), 9 primitives, `/styleguide` verified at 1440+390
- Depends on: M0
- Spec: gamified-competitive aesthetic; use `frontend-design` + `ui-ux-pro-max` skills
- Acceptance:
  - [x] Design tokens (palette, typography, spacing, radii, motion) defined once and consumed everywhere (Tailwind v4 `@theme` in `globals.css`; fonts via `next/font`).
  - [x] Core primitives in `src/components/`: button, card, badge/chip, input, modal, nav shell, page layout, stat/leaderboard row, toast.
  - [x] `/styleguide` dev page rendering every primitive in every state.
  - [x] Playwright screenshot evidence of `/styleguide` (desktop + mobile widths) in `.claude/debug-shots/`.

### M2: Identity — Sussex auth + GitHub connect + onboarding
- Status: `[x] done (2026-06-10, 91478d2)` — magic-link auth (Auth.js v5 + Drizzle), 2-step onboarding, guarded shell; 35 unit + 4 e2e green
- Depends on: M1
- Spec: CLAUDE.md → Binding v1 decisions (identity); PRD §6.1–6.2
- Acceptance:
  - [x] Pure domain predicate: email eligibility (`@sussex.ac.uk` only) — unit-tested first, including tricky cases (subdomains, casing, plus-addressing).
  - [x] Auth.js magic-link sign-in restricted by that predicate; dev email adapter logs the link to console (real SMTP → Needs from Sampo).
  - [x] Onboarding flow: job-role selection (drives pool filtering) + mandatory GitHub account connect (OAuth, read-only) behind a mockable `infra/github` adapter.
  - [x] Session-guarded app shell: signed-out → landing/sign-in; signed-in-but-unboarded → onboarding; complete → dashboard.
  - [x] E2E: full sign-up → onboard → dashboard journey with mocked email + GitHub.

## Phase B — Prize Pools (the core loop, end to end)

### M3: Pool domain kernel
- Status: `[x] done (2026-06-10, eb59509)` — `domain/prize-pools/`: time-driven lifecycle (tick + effects-as-data), entry guards, normalized-Borda vote aggregation; 77 tests
- Depends on: M0
- Spec: CLAUDE.md → Prize Pool lifecycle; PRD §12 #5, #16
- Acceptance:
  - [x] Pure state machine `draft → published → building → judging → closed` + `extended`/`cancelled`, with explicit guards (min 6 entrants, one +48h extension, time-driven transitions) — every transition and every guard unit-tested, written test-first.
  - [x] Entry rules pure: role/difficulty eligibility, soft cap 3 concurrent active pools.
  - [x] Vote-aggregation rule pure: ranked sets → final standings, judge-to-win eligibility filter, deterministic tie handling.

### M4: Pool persistence, manual import & approval queue
- Status: `[x] done (2026-06-10, 30bdbb2)` — schema (profiles/pools/entries), `pools:import` CLI + 15 starter specs, `/operator/pools` approval queue; 155 unit + 7 e2e green
- Depends on: M2, M3
- Spec: CLAUDE.md → AI generation layer (approval queue), pool spec sources; PRD §12 #10, #13
- Acceptance:
  - [x] Drizzle schema: users, profiles, pools, entries (typed end to end, migration committed).
  - [x] `npm run pools:import` reads `content/pools/*.md` (YAML-frontmatter entries), validates (clear per-entry error report), dedupes by slug, creates pools in `draft`.
  - [x] Documented manual-pool format + 3 starter specs per launch role committed as real content.
  - [x] Operator approval slice: list drafts → approve (`published`) / reject — operator-gated.
  - [x] Slice tests for import + approve; kernel rules reused, not duplicated.

### M5: Browse & join pools
- Status: `[x] done (2026-06-10, 3beed2e)` — credit kernel + ledger, `/pools` browse/detail, race-safe join, `pools:tick` lifecycle cron (extension→cancellation+refund verified live); 183 unit + 9 e2e green
- Depends on: M4
- Spec: PRD §5 steps 4–5; CLAUDE.md → pool lifecycle (entry guards)
- Acceptance:
  - [x] Pool listing filtered by the user's job role + difficulty; pool detail page (spec, window, entrants/cap).
  - [x] `join-pool` slice enforcing kernel guards (eligibility, cap 3, pool joinable); free-credit debit recorded.
  - [x] Lifecycle cron job (host-scheduled) executing time-driven transitions incl. extension/cancellation with credit refund.
  - [x] E2E: browse → join → see joined state; screenshot evidence.

### M6: Build window — fresh repo + submission
- Status: `[x] done (2026-06-10, 14876ea)` — repo-freshness + window kernel rules, `infra/github` push-signal read, `infra/video` (local dev fallback), `submit-entry` slice + UI; 233 unit + 11 e2e green
- Depends on: M5
- Spec: CLAUDE.md → Binding decisions (repo rule), anti-cheat (pools); PRD §6.2, §6.5
- Acceptance:
  - [x] `infra/github` reads repo metadata + push events (server-side signals only); mockable, rate-limit-aware.
  - [x] Pure predicate: repo freshness (created after window open) + in-window push timeline — unit-tested first.
  - [x] `submit-entry` slice: link repo (verified) + demo video upload via `infra/video` (Cloudflare Stream adapter; dev fallback stores file locally — real account → Needs from Sampo); deadline enforced by kernel.
  - [x] Submission UI on the pool page with verification feedback; e2e of the happy path.

### M7: Pool anti-cheat
- Status: `[x] done (2026-06-10, fd239e5)` — originality + moderation kernel, `infra/similarity` seam, scan-submissions (CLI + action) + review-flag slices, `/operator/flags` queue; 278 unit + 12 e2e green
- Depends on: M6
- Spec: CLAUDE.md → Anti-cheat (pools — authenticity, AI tools allowed)
- Acceptance:
  - [x] Duplicate/reuse detection across entries and a user's prior submissions (pure scoring predicate + infra similarity adapter).
  - [x] Flagging pipeline: flagged submissions excluded from judging/results pending operator review; review slice (uphold/clear).
  - [x] Every predicate unit-tested with realistic fixture telemetry; flagged-path e2e.

### M8: Peer judging
- Status: `[x] done (2026-06-10, 7aaa122)` — pure circulant judge-assignment kernel (balanced coverage, never-own, seeded), assign-judges slice wired into the cron, cast-vote slice + anonymized judging UI, aggregation via M3 kernel; 308 unit + 13 e2e green
- Depends on: M6
- Spec: CLAUDE.md → judging decisions; PRD §6.6, §12 #8
- Acceptance:
  - [x] Randomized, anonymized judge assignment (~5 per judge, scaled; never your own entry) — assignment rule pure + property-tested for coverage/fairness.
  - [x] Judging UI: watch ~30s demos (signed playback URLs), drag-rank best-to-worst, submit ranking.
  - [x] Aggregation via M3 kernel; judge-to-win eligibility enforced.
  - [x] E2E: multi-user judging round on seeded data; screenshots.

### M9: Close pool — results, XP, rank
- Status: `[x] done (2026-06-10, 04c8520)` — `domain/gamification/` kernel (XP/levels/rank, 39 tests), close-pool slice executing `finalize-results`, idempotent + race-safe award, `/pools/[id]/results` reveal; 347 unit + 14 e2e green
- Depends on: M8
- Spec: CLAUDE.md → Gamification (XP, levels, global rank)
- Acceptance:
  - [x] Gamification kernel: XP grants (join/submit/judge/win/streak), level curve, global-rank movement — pure, test-first, every rule covered.
  - [x] `close-pool` transition awards XP/rank/standings atomically; results page with placement reveal.
  - [x] Full-loop e2e: join → build (mocked GitHub) → submit → judge → close → results.

### M10: Profiles & leaderboards
- Status: `[x] done (2026-06-10, 83c578c)` — `domain/gamification/` badges + visibility kernel; `/u/[handle]` public profile, `/leaderboard` global + per-role, privacy toggle; 393 unit + 16 e2e green
- Depends on: M9
- Spec: CLAUDE.md → Profiles, Gamification; PRD §6.10, §12 #4, #19
- Acceptance:
  - [x] Public profile: rank, level/XP, wins, competition history, badges, streaks, linked GitHub, role(s); losses aggregate-only.
  - [x] Privacy toggle (public default → private hides from public view/leaderboards/search) — visibility rule pure + tested.
  - [x] Global leaderboard + per-role filtered views (per-role results captured since M9).
  - [x] First badge set + streak rules as data + pure predicates; shareable profile URL; screenshots.

## Phase C — Live Code Battles

### M11: Battle domain kernel
- Status: `[x] done (2026-06-11, 13535c6)` — `domain/battles/` (lifecycle + speed/penalty scoring + Phase-2 wager rules) + `domain/gamification/` Elo & battle-XP; 164 new kernel tests, all pure
- Depends on: M0
- Spec: CLAUDE.md → Battle lifecycle, scoring, Elo
- Acceptance:
  - [x] Pure state machine `challenged/queued → matched → countdown → live → resolved` + `voided`/`forfeited`/`flagged` — every transition/guard unit-tested.
  - [x] Speed+penalty scoring pure fn `(submissionHistory, timeLimit, penaltyPerWrong) → outcome` — decisive-win, timeout-partial, tie, draw all covered; submission cooldown rule.
  - [x] Elo pure fns (K-factor, expected score, floor, inactivity) + battle-XP grants; wager-settlement rules designed + unit-tested now, wired to nothing (Phase 2).

### M12: Judge0 + problem bank
- Status: `[x] done (2026-06-11, 54e9e11)` — network-denied Judge0 stack, `infra/judge` + `infra/ai` seams, draft→verify→approve pipeline, `/operator/problems` queue; 32 problems machine-verified in real Judge0 and approved; 592 unit + 16 e2e green
- Depends on: M11
- Spec: CLAUDE.md → Stack (Judge0), Binding decisions (problem bank)
- Acceptance:
  - [x] `docker compose up judge0` runs locally, resource-capped + network-denied; `infra/judge` client (submit, poll verdict) mockable.
  - [x] Problem schema: statement, difficulty tier, reference solution, hidden tests, status (draft/approved/retired).
  - [x] AI drafting pipeline (Vercel AI SDK behind `infra/ai`): draft → auto-verify reference solution passes its own tests in Judge0 → operator approval queue (reuses M4 pattern).
  - [x] ≥30 approved problems across 3 tiers seeded via the pipeline; retirement/rotation supported.

### M13: Realtime service
- Status: `[x] done (2026-06-11, 26ddb53)` — `src/realtime/` WS service (kernel-driven rooms, auth seam, exact-instant scheduling) + `lib/match-events` typed contract; simultaneity integration-tested over real sockets (1ms skew live); 634 unit green (+42)
- Depends on: M11
- Spec: CLAUDE.md → realtime/ (transport only, never authoritative)
- Acceptance:
  - [x] Standalone WS service (`npm run dev:ws`): auth handshake, match rooms, presence, ready signals, synchronized countdown/"go", match timer, disconnect/reconnect grace tracking.
  - [x] Typed match-event contract in `lib/` shared by service + client.
  - [x] Simultaneity verified by an integration test (two simulated clients receive "go"/problem within tolerance) — it's a correctness property.
  - [x] All authoritative decisions delegated to slices/kernel; service holds no business rules.

### M14: Battle arena UI
- Status: `[x] done (2026-06-11, 386b793)` — full-phase arena (lobby→countdown→live→settled) on a pure client reducer; paste-block + focus telemetry over a new server-stamped wire event; mocked room = real BattleRoom+kernel in-browser; 663 unit + 18 e2e green
- Depends on: M1, M13
- Spec: CLAUDE.md → anti-cheat (battles, in-match); design system
- Acceptance:
  - [x] Arena: code editor (multi-language), problem pane, match timer, opponent progress (tests passed), submission verdict feed.
  - [x] Paste-blocking + tab/focus-blur telemetry captured and sent (in-match anti-cheat inputs).
  - [x] Countdown → reveal → live flow against a mocked room; screenshots of every match phase.

### M15: Battle slices — end to end
- Status: `[x] done (2026-06-11, pending)` — all five slices + settlement engine, DB-rendezvous room materialization, lobby `/battles`, queue matchmaking live in dev:ws; 746 unit + 20 e2e green
- Depends on: M12, M13, M14
- Spec: CLAUDE.md → Battle lifecycle, Binding decisions (entry paths)
- Acceptance:
  - [x] Slices: `send-challenge`/`accept-challenge` (username/link), `enter-queue` (simple pairing: prefer Elo proximity, widen fast) + online-players list, `submit-solution` (cooldown → Judge0 → verdict into kernel), `resolve-battle` (Elo/XP, history), forfeit on grace expiry, void on no-show.
  - [x] Judge0 verdict authoritative — WS "finished" events decide nothing (tested).
  - [x] E2E: two browser contexts fight a full battle (challenge → countdown → solve → resolved → Elo movement) on a seeded problem.

### M16: Battle anti-cheat + ladder
- Status: `[ ]`
- Depends on: M15
- Spec: CLAUDE.md → anti-cheat (battles, post-match); gamification (Elo ladder)
- Acceptance:
  - [ ] Post-match: plagiarism diff vs bank solutions + opponent, AI-likelihood heuristics, cadence anomalies — pure predicates over telemetry fixtures, test-first.
  - [ ] Any signal → `flagged` for operator review; confirmed → forfeit + Elo penalty + escalating bans (rules tested).
  - [ ] Battle Elo ladder page; battle badges (streaks, first blood, giant-killer) live on profiles.

## Phase D — AI generation, deploy

### M17: AI pool generation
- Status: `[ ]`
- Depends on: M9
- Spec: CLAUDE.md → AI generation layer; PRD §6.4A
- Acceptance:
  - [ ] Per-spec engagement metrics collected (joins, completion, drop-off, judge engagement).
  - [ ] Pure generation policy: signals → pool spec params (theme/difficulty/cadence) — unit-tested decision table.
  - [ ] Scheduled `generate-pool` slice drafts content via `infra/ai` (mocked in tests) → M4 approval queue; AI-created pools downstream-identical to manual ones (tested).

### M18: Production deploy
- Status: `[ ]`
- Depends on: M10, M16
- Spec: CLAUDE.md → Hosting (container host + Cloudflare)
- Acceptance:
  - [ ] Container host (Railway-class) running app, Postgres, WS service, network-denied Judge0; Cloudflare DNS/CDN/proxy in front (account/domain → Needs from Sampo).
  - [ ] Real adapters wired where credentials exist (email, GitHub OAuth, Cloudflare Stream); secrets in host env, never the repo.
  - [ ] Migrations + cron transitions running in prod; deploy steps documented in CLAUDE.md (Commands updated).
  - [ ] Smoke e2e against the deployed URL: sign-up gate + a battle countdown.

## Phase E — Marketing

### M19: Landing page + campus launch kit
- Status: `[ ]`
- Depends on: M18
- Spec: PRD §10.5 (cold start), §3 (personas); design system
- Acceptance:
  - [ ] Public landing page: thesis ("prove you can ship"), both modes shown, profile showcase, Sussex-only positioning, sign-up CTA — `frontend-design` quality bar, screenshots desktop+mobile.
  - [ ] SEO/OG meta + shareable profile cards.
  - [ ] Campus launch kit in `marketing/`: poster/flyer copy, CompSoc/Discord + lecture-shoutout blurbs, launch-event pool concept ("Freshers' Build Night"), first-2-weeks content calendar seeded with real pool/battle events.

---

## Needs from Sampo

| Need | For | Status |
|------|-----|--------|
| GitHub OAuth app (client id/secret) | M2 real GitHub connect (mocked until then) | open |
| SMTP/email provider creds | M2 real magic-link delivery (console adapter until then) | **wired** 2026-06-10 — IONOS (`smtp.ionos.co.uk`) in `.env`; `SmtpEmailClient` behind the email seam, auto-active in prod, `npm run email:verify` confirmed conn+auth. Dev still uses the outbox. |
| Cloudflare account (Stream + DNS) + domain name | M6 video, M8 demo playback, M18 deploy | open — M6 ships on the `LocalVideoClient` dev fallback (writes demos to `.dev/videos/`); M8 renders the judging `<video>` player wired to each entry's playback URL and enforces "only assigned judges get the URL" server-side (the access-control half of "signed playback"), showing a placeholder in dev. The real Stream client (direct upload + signed playback URLs) is gated behind `CLOUDFLARE_*` and lands at M18. |
| Anthropic API key (build-time generation) | M12, M17 | open — M12 ships on the deterministic curated drafter (32 hand-authored fixtures, still machine-verified through the same pipeline); with `ANTHROPIC_API_KEY` set, `getProblemDrafter` switches to the real Claude drafter (`claude-opus-4-8` via Vercel AI SDK) with zero code change. |
| Real code-similarity backend (clone repos + shingle/MinHash the file tree) | M7 anti-cheat depth | open — M7 ships on the identity-based `LocalSimilarityClient` (two entries on the same repo score 1.0, the blatant collusion/reuse case). It catches duplicate repo submissions but not copied code in a fresh repo; the real content-shingling client slots behind the `infra/similarity` seam unchanged. |
| Container host account (Railway or Fly) | M18 | open |
| Confirm battle languages for v1 (suggest: Python, JS/TS, Java, C++) | M12 problem bank | open — M12 pinned the suggested set (`python/javascript/typescript/java/cpp`) as the kernel `BATTLE_LANGUAGES` union + Judge0 language-id map; changing the set is a one-line kernel edit + test update. |
| Decide: local docker Postgres vs Supabase — `.env` was found pointing at a Supabase URL whose host no longer resolves (dead project?). Restored to the docker db so `db:*`/dev/tests work; the Supabase line is kept commented in `.env`. If Supabase is the plan, revive/recreate the project and we'll wire it properly. | dev DB (all milestones) | open |

## Build log

*(one line per completed milestone — date, milestone, summary, commit)*

- 2026-06-11 — **M15: Battle slices — end to end** — battles become a real, playable mode: every entry path, the judge path, and settlement, wired across BOTH processes with one rule — the DB row is the authority and the rendezvous. **Kernel** (test-first): `domain/battles/matchmaking.ts` (14 tests) — `pairQueue` is a greedy longest-waiter-first fold, each seeker taking the Elo-closest opponent inside `allowedEloGap` (base 150, +25/s waited — "prefer Elo proximity, widen fast" as two dials), deterministic (equidistant ties → longer waiter → userId; order-independence swept) so the re-running tick is idempotent over an unchanged queue; throws on duplicate userIds (a corrupt queue must never self-pair). `ACTIVE_BATTLE_STATUSES` (matched/countdown/live) joins lifecycle.ts — the "one battle in motion at a time" guard, pending challenges deliberately NOT commitments. **Migration 0010**: `battles` (kernel-typed status/outcome columns; mid-flight goAt/status mirrored by the realtime executor so a restarted service rebuilds rooms; settled fields written exactly once), `battle_submissions` (FULL history retained — CLAUDE.md anti-cheat + the scoring kernel's input + M16's plagiarism corpus), `battle_results` (the pool_results twin: unique (battle,user) idempotency lock, eloBefore/eloAfter pin exactly what `applyBattleElo` did), `battle_queue` (PK = one ticket per user); `profiles` gains elo (default `ELO_START`)/battleGames/battleStreak/lastSeenAt. **resolve-battle** (10 tests) — the single settlement engine EVERY conclusion funnels through (decisive from submit-solution, timeout/forfeit/void from the realtime executor, decline/cancel voids): callers name only HOW it ended (`BattleSettlement`), never who won — the scoring kernel derives the winner from the persisted history on both scored paths, which is what makes the Judge0 verdict the sole authority; `settle-deps` claims the row with a CONDITIONAL status update (the idempotency lock — a decisive submit racing the timeout tick serializes here, the loser writes nothing), locks both profiles in userId order (no deadlocks), applies Elo/XP/streak via the kernel and inserts results in ONE transaction. **submit-solution** (10 tests) — guards → COOLDOWN BEFORE a judge run is spent → judge → record → settle only on a full pass; a judge infra failure is an `error` outcome with NO record and no penalty (our failure, not the player's); "WS finished events decide nothing" pinned three ways (no such event exists in the contract; room test: a `progress` claim of any size changes no status; slice test: a failing verdict settles nothing). **Entry slices**: send-challenge (`parseChallengeTarget` accepts handle/@handle/profile-link; self-challenge + duplicate-pending guards; cancel = void via the engine), accept-challenge (busy guard BOTH seats, random approved problem drawn AT match time, kernel `matchBattle` ready window, conditional activation so double-accepts lose cleanly; decline = void), enter-queue/leave-queue + match-queue (the pairing tick: pure pairQueue + matchBattle, `claimQueuePair` deletes both tickets + inserts the matched battle atomically — a player who left mid-tick conflicts the pair instead of being matched). **Realtime** (M13 service grown up, 14 new tests incl. integration): rooms materialize LAZILY from the battles row on first join (in-flight dedup so simultaneous joins share one room — no create-room RPC exists, crash recovery free: a rebuilt room's clock immediately fires any past deadline); `onEffects` now carries the room's outcome (forfeit winner/reason), the executor (`room-source.ts`) mirrors countdown/live to the row and routes record-result/apply-ratings into settleBattle with the room's telemetry; `settleFromAuthority` (kernel resolveDecisive, effects deliberately NOT re-forwarded) lets the Next-side decisive settle broadcast through the room via a new internal HTTP surface on the same port (`/healthz` + `POST /internal/battles/:id/settled` → replies with the telemetry log, which the Next side persists — fire-and-forget `infra/realtime/notify`, DB result stands if the service is down); the matchmaking tick runs in dev:ws every 3s (CLAUDE.md: realtime service, not cron), verified live (`matchmaking: paired 1 battle(s)`, queue→matched with problem + deadline, tickets consumed). **UI**: `/battles` lobby (Elo/record/streak cards, challenge form, incoming/outgoing with accept/decline/cancel, queue toggle, online players via a lastSeenAt heartbeat — presence is deliberately boring for a campus population — and recent battles with Elo deltas), all server-rendered with ONE client poller (4s heartbeat: marks online, bounces you into a battle the moment one goes into motion, refreshes only when a fingerprint changes); `/battles/[id]` real path: player gate (no spectating in v1), settled → server-rendered match record (Elo movement + XP), live → Arena over the viewer's own session token with `serverSubmit` wiring the real action; the winner lands on the record automatically (Next's post-action route refresh — a rejected submission re-renders the still-live arena with client state preserved); Battles in MAIN_NAV, dashboard Battle Elo card real + arena CTA; nav-shell phantom-scrollbar fix (the active underline hung below the nav box, making overflow-x-auto vertically scrollable on Windows — it now sits at the box's bottom edge). **E2E** (battle-flow.spec, 2 journeys; playwright boots BOTH servers — dev:ws readiness via /healthz): the full arc — A challenges B by handle → B's polled lobby surfaces it → accept → both bounced into the arena → ready-up → synchronized countdown → simultaneous reveal → B TYPES a JS solution (paste blocked is the honest path) → local-judge verdict 3/3 → B lands on Victory + `1200 → 1220 (+20)` + `+30 XP`, A hears Defeat over the socket and reloads to `1200 → 1180 (-20)` → B's lobby shows Elo 1220 + `+20 Elo` history; and decline-voids-with-nothing-rated. Debugging this surfaced a real infra bug worth recording: **the judged child process inherited FORCE_COLOR=1 from Playwright's webServer**, making node's console.log wrap numbers in ANSI codes (`[33m3[39m`) — every correct solution judged wrong-answer; the local judge now runs solutions with a sanitized env (NODE_OPTIONS/FORCE_COLOR stripped, NO_COLOR set) and spawns node via `process.execPath` not PATH. `JUDGE_FORCE_LOCAL=1` (dev-gated) exists because Next's env loader refills empty-string overrides from .env — "unset JUDGE0_URL for this server" cannot be expressed, a positive flag can; `E2E_FORCE_PROBLEM_SLUG` pins the match-time problem draw for the spec. Gates: typecheck + lint clean, 746 unit green (+83), 20 e2e green serially (full-suite runs on this machine rotate 3-4 timeout-shaped flakes under compile contention — every spec green in serial isolation, the M9/M10/M12-documented pattern). Commit `pending`.
- 2026-06-11 — **M14: Battle arena UI** — the client of everything M13 built: the full match surface, organised so every phase the arena can render is decided by a PURE function. **`features/battles/arena/arena-state.ts`** (22 tests, written first) — the client mirror of the room: `reduceArena` folds typed `ServerEvent`s into UI state (connecting → lobby → countdown → live → settled), with the reveal discipline pinned the same way the room pins it (a countdown resync NEVER carries the problem; a live resync restores it — reconnect recovery tested both ways); `describeResult` interprets the raw settled result relative to MY side (winner=me → won, voided → "nothing rated", resolved+null → draw/timeout never a loss); `formatClock` clamps negatives (a late tick must not render -0:01). **Telemetry wire event** (contract + room + WS edge): new `telemetry` client event with `TELEMETRY_KINDS` (`paste-blocked`/`focus-lost`/`focus-regained`) — the client names only the KIND; the SERVER stamps the time (a client-supplied timestamp is stripped at parse, pinned by test — the never-trust-client-clocks posture as GitHub repo signals); `room.recordTelemetry` (4 new room tests) records live-only as seconds-from-go, fire-and-forget (no error echo — pre-live blur noise must not become an error storm), NEVER relayed to the opponent (a cheat signal must not tip anyone off), exposed via `telemetryLog` for M15 to persist with the result. **The mocked room** (`mock-room.ts`) is barely a mock: it instantiates the REAL `BattleRoom` + M11 kernel in-browser with the real clock — countdown, synchronized go, timer syncs, void-on-quit are all genuine; simulated is only what M15 adds (the phantom opponent behind a dev-controls strip, and a scripted judge whose full pass fabricates the `battle-status resolved` the real submit-solution slice will broadcast after `resolveDecisive`). The arena consumes both worlds through one `ArenaDriver` seam — `connection.ts` is the real-WebSocket implementation (hello→join handshake; outbound frames round-trip the STRICT parser so the client can't emit a frame the server would reject). **UI** (`Arena.tsx` + `CodeEditor.tsx`): CodeMirror 6 editor skinned in the arena-terminal tokens, all 5 kernel `BATTLE_LANGUAGES`, paste AND drop blocked at the editor with each attempt recorded+sent; window blur/focus listeners armed only while live; problem pane (tier badge, statement), header match timer ticking from server-authoritative `endsAt` (snap-on-phase-entry fixed a screenshot-caught defect: stale mount-time clock rendered "6" on the 5s countdown), opponent progress pips relayed live-only, verdict feed (submissions + "recorded" anti-cheat notices, newest first), cooldown-disabled submit, full-bleed `bg-grid` layout with clip-corner plates. Page `/battles/[battleId]` is thin wiring: prod → 404 until M15; dev `?mock=1` → mocked room; dev bare → real socket to `dev:ws` (`?as=dev-a/dev-b` picks a demo seat). E2E (2 journeys, 18 total green serially): the full arc — lobby ready-up → countdown → reveal → typed solution → synthetic paste BLOCKED (content unchanged, "recorded" in feed) → blur recorded → opponent progress 2 → submit rejected 2/5 → cooldown brake → accepted 5/5 → Victory; and opponent-quits-pre-reveal → "Match voided / no rating change". Real-socket path verified live: two browser tabs on the dev:ws demo room both went live with the identical problem + running timer (screenshot `m14-ws-live-real-socket.png`). Screenshots `.claude/debug-shots/m14-{lobby,countdown,live,victory,voided,ws-live-real-socket}.png`. New deps: `@uiw/react-codemirror` + per-language CodeMirror packages. Gates: typecheck + lint clean, 663 unit green (+29). Commit `386b793`.
- 2026-06-11 — **M13: Realtime service** — the battles transport layer, built to one discipline: THE ROOM OWNS SOCKETS AND CLOCKS, THE KERNEL OWNS RULES — every transition in `src/realtime/` is produced by an M11 kernel call (`markReady`, `tickBattle`, `abortBeforeReveal`, `forfeitBattle`) and a `status =` assignment that didn't come from a kernel result is named a review failure in the room's header comment. **`lib/match-events.ts`** (13 tests) — the typed wire contract shared by service + every client (the M14 arena imports the same types, so the two sides can't drift): client events (`hello/join/ready/quit/progress`) cross `parseClientEvent`, strict on shape (a WS frame is UNTRUSTED input, same posture as a Judge0 verdict) but tolerant of unknown EXTRA fields (forward compat — accepted events are rebuilt field-by-field so extras never travel); server events reuse the kernel unions (`BattleStatus`/`PlayerSide`/`ProblemTier` — typed end to end like the DB schema); `room-state` is the (re)join resync and carries `problem: null` until live — holding the statement back until the synchronized reveal is a transport duty the RECONNECT path must honor too, pinned by test. **`realtime/room.ts`** (20 tests, written first against an injected clock/scheduler/sends — no sockets): joins (players only; stranger → `not-a-player` and learns nothing), presence broadcasts, ready → kernel `markReady` (second ready starts the countdown — both clients receive the byte-identical `goAt`), time-driven edges scheduled at EXACT instants rather than a polling interval (ready-deadline no-show → kernel void; `goAt` → the go, ONE payload built once and both sends in the same event-loop turn; time limit → kernel resolve with `winner: null` — scoring over submissions is M15's job, transport announces only); server-authoritative `timer` re-syncs every 10s (resolve scheduled BEFORE the syncs so a tie at the deadline settles first); disconnect rules by phase — during `matched` nothing (the ready deadline arbitrates no-shows), during `countdown` → kernel `abortBeforeReveal` void (binding: nothing revealed → nothing rated), during `live` → a DISCONNECT_GRACE_SECONDS clock the room tracks while the forfeit DECISION routes through kernel `forfeitBattle` (reconnect inside the window cancels it — tested both ways); `quit` live → forfeit to the opponent, quit pre-reveal → void; `progress` relays tests-passed to the OPPONENT only, live only; every kernel effects list is forwarded VERBATIM to an injected `onEffects` executor (M13 logs, M15's slices will execute `record-result`/`apply-ratings`) — that seam is acceptance criterion 4 made concrete. **`realtime/auth.ts`** (6 tests) — who is on the socket: `Authenticator` seam; `DevTokenAuthenticator` (`dev:<userId>`, the WS twin of /dev/login, hard-gated inside the call so a mis-wired prod deploy fails CLOSED); `DbSessionAuthenticator` validates real Auth.js session tokens (injected lookup + clock; expiry inclusive; `forDb()` wires Drizzle lazily so the driver stays off the test path). **`realtime/server.ts`** — the ws edge: per-socket transport state machine only (hello-first with a 10s drop timeout, join after auth, route ready/quit/progress into the room), `RoomRegistry` (M15's slices create rooms here at the `matched` transition). **Simultaneity integration test** (3 tests, real server on an ephemeral port, two real `ws` clients): both receive the IDENTICAL `countdown.goAt` and deep-equal `go` payloads, skew bounded < 150ms, and the go never fires early; handshake enforcement + malformed-frame rejection over the live socket. `npm run dev:ws` (CLAUDE.md Commands updated) verified live with a two-client probe: identical goAt on both, go at +5.000s with **1ms** inter-client skew, progress relayed, kernel effects logged (`start-countdown → countdown`, `reveal-problem, start-match-timer → live`). New deps: `ws` (+ types). Gates: typecheck + lint clean, 634 unit green (+42). No schema, no UI — the arena (M14) and the battle slices (M15) plug into the registry + contract built here. Commit `26ddb53`.
- 2026-06-11 — **M12: Judge0 + problem bank** — the binding "AI-drafted, machine-verified, human-approved" pipeline, end to end. *(Resumed from a lost session: the full milestone existed on disk uncommitted with the roadmap still `[ ]` — audited every file, fixed one lint warning (dead `totalApproved` counter in seed.ts), then gated + verified live; unrelated session noise (`.agents/`, higgsfield skills, `skills-lock.json`, `.mcp.json` MCP additions) deliberately left out of the commit.)* **Kernel** `domain/battles/problems.ts` (29 tests): 3 tiers (`easy/medium/hard` — deliberately distinct from rank-gated pool difficulty; battle tier is a content scale picked at match time), status space `draft → approved → retired`, the v1 language union `python/javascript/typescript/java/cpp` (pending Sampo's confirmation, pinned by test), and `checkProblemSpec` collecting ALL failed reasons (the `checkJoin` house style) — slug shape, statement, tier, language, reference solution, ≥3 hidden tests with distinct inputs (an identical input grades nothing and inflates "tests passed"); `approveProblem`/`retireProblem` mirror the pool approve gate; problems are LANGUAGE-AGNOSTIC stdin/stdout IO pairs so ONE reference solution verifies tests any-language players run against (M15). **Judge0** in docker-compose under a `judge0` profile (default `up` never starts it): api + workers + own postgres + redis, ALL on `judge0net` — an `internal: true` Docker network with no route out — workers (which execute untrusted code) live ONLY there, the api also joins default purely so the host reaches :2358; `ENABLE_NETWORK: false` + `ALLOW_ENABLE_NETWORK: false` (clients can't even request network) = defence in depth, proven live: a submission calling `urlopen()` got `URLError` inside the sandbox; per-service cpu/mem caps + per-submission CPU 5s / memory 512MB (per-thread accounting because cgroup-v2 Docker Desktop kills isolate's `--cg` mode — sandbox unaffected, only accounting changes). **`infra/judge` seam** (13 tests): typed `JudgeClient` (submit/poll/run) with pure `verdict.ts` (Judge0 status table → `TestVerdict`, unknown statuses map to `internal-error` so nothing unrecognized silently passes; `aggregateRun` folds per-test results into the `passedAll`/`testsPassed` shape M11 scoring consumes); `Judge0Client` (batch REST, base64 both ways), `LocalProcessJudgeClient` (dev-only unsandboxed node/python runner so the pipeline stays machine-checked without docker — NEVER for player code, the factory prefers Judge0 whenever `JUDGE0_URL` is set), `MockJudgeClient` for slice tests. **`infra/ai` problem drafter seam** (3 tests): `ProblemDrafter` contract with `source: 'curated'|'ai'`; `CuratedProblemDrafter` (deterministic, 32 hand-authored fixtures: 12 easy/12 medium/8 hard, all-Python references so both judges can verify); `AnthropicProblemDrafter` — Claude `claude-opus-4-8` (high-stakes generation per CLAUDE.md; `claude-api` skill consulted) via Vercel AI SDK `generateObject` + zod schema, dynamically imported so the SDK stays off the no-key path, the policy stamps the requested tier (the model never decides tier); deliberately NO `infra/ai` barrel — each AI use case owns its surface so the M17 pool drafter can't couple. **`draft-problem` slice** (7 tests): draft → kernel-validate (BEFORE spending a judge run) → VERIFY the reference solution against its OWN hidden tests → persist survivors as verified drafts; in-batch slug claim set + DB `onConflictDoNothing` = idempotent at both layers; skip report (`invalid`/`duplicate-slug`/`verification-failed`) for the operator/seed log. **`approve-draft` slice** (12 tests): operator approve/reject/retire over the kernel transitions plus ONE infra precondition the kernel can't know — `verifiedAt` must be set (an unverified draft can never be approved); rejection is archival (`rejectedAt`, status stays `draft`, slug stays claimed — the pools pattern); retire = the rotation duty for leaked problems. Migration `0009`: `problems` table, kernel-union-typed columns, unique slug. `/operator/problems` queue (OPERATOR_NAV): drafts with statement + collapsible reference solution + approve/reject, bank with retire, retired history; operator re-checked in every action (public endpoints). `npm run problems:seed` (CLAUDE.md Commands updated) runs the REAL pipeline then approves — verified live against REAL Judge0: all 32 fixtures drafted+verified+approved (`bank: 32 approved (easy: 12, medium: 12, hard: 8)` ≥ 30), re-run is a no-op, approve/retire exercised through the UI (screenshots `.claude/debug-shots/m12-*.png`). Gates: typecheck + lint clean, 592 unit green (+58), 16 e2e green serially — two e2e failures investigated and proven ENVIRONMENTAL, no code change: a stale `.next` cache made every server action 500 (`clientReferenceManifest` Next dev invariant; fixed by wiping `.next`), and close.spec blew its 30s budget only while the four Judge0 containers competed for CPU (18s once stopped). Commit `54e9e11`.
- 2026-06-11 — **M11: Battle domain kernel** — Phase C opens with the PURE battle kernel: every authoritative battle rule as plain-data functions, zero infra/framework imports, so M12–M15 (Judge0, realtime, arena, slices) plug transport and I/O into rules that are already proven. *(Resumed from a lost session: lifecycle/scoring/wager modules existed with passing tests; `elo.test.ts`/`battle-xp.test.ts` existed with NO implementations — the interrupted test-first step. Audit kept the existing modules, fixed two gate failures the lost session never hit — `scoring.ts` didn't survive `noUncheckedIndexedAccess`, and `forfeitBattle` had an unused `_reason` param, now returned as `reason` in the result so slices can record WHY a forfeit happened — and closed one real gap: the table declared `countdown → voided` but no kernel move could produce it.)* `domain/battles/lifecycle.ts` (90 tests) — explicit `BATTLE_TRANSITIONS` table (`challenged`/`queued → matched → countdown → live → resolved` + `voided`/`forfeited`/`flagged`), every edge allowed AND every non-edge forbidden by an exhaustive sweep; event moves return snapshot + effects-as-data (`matchBattle` — the Phase-2 escrow effect slots at this transition, v1 mandates nothing; `markReady` — the SECOND ready schedules the synchronized `goAt`, deadline inclusive; `abortBeforeReveal` — NEW, the event-driven void from matched/countdown (disconnect during countdown, delivery failure) honoring "no-show before problem reveal → voided"; `resolveDecisive` — the Judge0-verdict path, a WS "finished" event never reaches the kernel; `forfeitBattle` — live-only, names winner + reason; `flagBattle` — resolved/forfeited only, Elo/XP stay applied, M16 owns the review move); time-driven edges in `tickBattle` (the M3 `tickPool` pattern): ready-deadline no-show → `voided`, `goAt` fires → `live` (effects `reveal-problem`+`start-match-timer`), time-limit → `resolved`; the binding "a void NEVER touches ratings/results" is pinned as an invariant swept over every void-producing path. `scoring.ts` (25 tests) — pure `scoreBattle(history, timeLimit, penaltyPerWrong) → outcome` in seconds-from-go (clock-free): decisive = earliest in-window full solve, and PENALTY NEVER OVERTURNS A DECISIVE WIN is swept across penalty 0→100k; timeout = most tests passed → lowest penalty-adjusted time → draw; the counting submission is the earliest occurrence of a player's best result so judge-spam after it changes nothing; zero-tests-by-both is ALWAYS a draw (spamming rejections must not beat silence); deadline instant closed like every pool deadline; order-independent + throws on corrupt input; `submissionCooldownRemaining` is the judge-spam brake the M15 slice checks BEFORE spending a Judge0 run. `wager-settlement.ts` (12 tests) — PHASE 2 DESIGNED NOW, WIRED TO NOTHING (deliberately excluded from the barrel; nothing outside its tests references it): equal stakes in integer minor units, rake as a basis-points PARAMETER rounding DOWN, win/forfeit pay the winner pot−rake, draw/void refund in full (the house never profits from a non-result), flagged HOLDS settlement; pot conservation (payouts+rake === 2×stake) swept as an invariant. `domain/gamification/elo.ts` (23 tests, implemented to the orphaned spec-tests) — the SEPARATE can-go-down rating pool rank deliberately isn't: logistic `expectedScore` (400-scale, complementarity + monotonicity swept), `kFactor` provisional 40 → established 24 after 10 games, `applyBattleElo` derives B's delta by NEGATING A's rounded delta when Ks match (independent rounding can leak a point — exact zero-sum is the tested invariant; sanctioned breaks: mixed K, floor 800 catching a loser), upsets move more than expected results, `decayedRating` drifts dormant HIGH ratings toward `ELO_START` 1200 per full week past a 28-day grace, never below start (inactivity is not a skill signal). `battle-xp.ts` (14 tests) — battle grants DISTINCT from pool grants, same philosophy (XP rewards activity, Elo measures skill): participation 5 / win +25, pinned RELATIVE to the pool economy (a max-streak battle win 55 < `WIN_XP_BASE` 200 — minutes vs days; win > pool join, < pool submit+join), forfeit earns NOTHING, `battleStreakXp` steps 5 capped at 5, `advanceBattleStreak` explicit reset rules (completed extends — win/lose/draw; forfeit resets; VOID touches nothing, mirroring cancelled pools). Barrels: `domain/battles/index.ts` (wager-settlement excluded by design) + gamification exports. No schema, no slices, no UI — kernel only, as scoped. Gates: typecheck + lint clean, 534 unit green (+164 for M11; note: the pre-M11 suite measures 370 at HEAD today, not the 393 the M10 log recorded — re-verified by stashing all M11 work and re-running, every HEAD test passes, nothing regressed; the M10 figure looks miscounted). Commit `13535c6`.
- 2026-06-10 — **M10: Profiles & leaderboards** — the identity surface the whole product renders onto (CLAUDE.md → Profiles: "Public by default, the recruiter-facing portfolio is the thesis"). Two new PURE kernel modules in `domain/gamification/` (test-first): `badges.ts` (20 tests) — the badge catalogue is DATA, a list of `BadgeDef` each owning a pure `earned(stats)` predicate, so adding a badge is a data edit not a code path; v1 pool set of 9 across bronze/silver/gold tiers (First Steps/Shipper/On the Board → Podium/On Fire/Veteran → Champion/Dominant/Unstoppable), `earnedBadges`/`earnedBadgeIds` filter the catalogue, and `badgeStatsFrom({profile, results})` folds closed-pool results + profile into the `BadgeStats` the predicates read; the binding properties are pinned as invariants not examples (unique ids, MONOTONICITY — a better record never revokes a badge — swept over a weak vs strong stat pair), thresholds are tunable dials; `visibility.ts` (12 tests) — the privacy rule as pure fns: `canViewProfile({visibility, isOwner})` (owner always, others only if public), `appearsInLeaderboard` (public-only, no per-viewer exception because the board IS a public surface), `toggleVisibility`, `DEFAULT_VISIBILITY='public'` (the recruiter thesis). One decision lives in the kernel and governs the profile page, both leaderboards AND search so they can't drift. Migration `0008`: `profiles.visibility` (text, default 'public' → every pre-M10 row is public, matching the default-public stance), value space = the kernel union. `toggle-privacy` slice (3 tests): `setProfileVisibility(deps, userId, visibility)` validates the requested value against the kernel union (an out-of-band POST can't write garbage) and persists via a mockable dep; the action takes the TARGET from the form (the toggle computes the opposite of current) so a double-submit is IDEMPOTENT, and the target user is ALWAYS the session — this endpoint can only ever toggle your own privacy. `view-profile` read model (`getProfileByHandle(handle, viewerUserId)`): resolves the public handle (linked GitHub username) → owner, gates on `canViewProfile` (private → `{kind:'private'}` notice for non-owners, full assembly for the owner), then builds the surface from `pool_results` ⋈ `pools` — level/XP (via `levelProgress`)/rank/streak, aggregate win/podium/ship stats (`badgeStatsFrom`), `earnedBadges`, and competition history. "Losses appear in aggregate stats only" is enforced HERE, in the read model: a non-podium placement counts into the aggregates but its rank is STRIPPED from the history row (`podiumPlacement` null outside the top 3), so the timeline shows "🏆 1st"/"Podium · Nth"/"Shipped"/"Competed" but never "you came 9th". `view-leaderboard` read models: GLOBAL board reads the authoritative `profiles.globalRank` (XP tie-break) the close-pool slice maintains; PER-ROLE boards (`?role=…`) are computed straight from `pool_results` summing in-role rank points + XP + wins (faithful to "filtered views computed from pool results, captured per-role since M9"); both apply the privacy filter `visibility='public'` IN SQL so a private account never even leaves the DB as a board row. UI (all server-rendered, the portfolio must be crawlable/JS-off): `/u/[handle]` profile page is intentionally viewable SIGNED OUT (the thesis is a public, linkable, OG-tagged identity surface — `generateMetadata` for shareability; session read only decides ownership + "you" highlight, never gates access); `ProfileView` (identity header + handle/role/visibility/gh-link, four headline StatCards, the badges grid earned-vs-locked, the history timeline); `PrivacyToggle` is a plain server-action form (no client JS); `/leaderboard` with the Global + per-role tab switcher (plain `?role=` links, deep-linkable) over `LeaderboardRow` primitives each linking to `/u/[handle]`. `Leaderboard` added to `MAIN_NAV`; dashboard gains a "View profile" link (kept the `gh:` badge — its removal broke identity.spec, restored). E2E `profile.spec` (2 journeys): (1) a public profile renders the portfolio (Champion badge, "🏆 1st" history, pool link) while a ship-no-podium profile shows "Shipped" with NO trophy/losing rank (aggregate-only proven), and the global board ranks champ>runner>tail by `a[href^="/u/"]` DOM order with the per-role Backend tab listing the winner; (2) the privacy toggle — as the owner, Make private → the `Private` badge + Make public appear, the account VANISHES from the leaderboard (`a[href]` count 0), and a different viewer sees only "This profile is private". New seed helpers (`seedClosedPool`/`seedRankedUser`/`addPoolResult`/`userIdByEmail`/`setProfileNumbers`) seed finalized results directly (close path itself is close.spec's job). 393 unit + 16 e2e green (serial; one transient identity/operator cold-compile flake passed on retry — the known parallel-compile flakiness, unrelated); screenshots `.claude/debug-shots/m10-*.png` (champion profile, global board, per-role board, private state). Commit `83c578c`.
- 2026-06-10 — **M9: Close pool — results, XP, rank** — the back half of `judging → closed`: turning a judged pool into awarded results. New PURE kernel `domain/gamification/` (test-first, 39 tests), pools-only (battles bring their own grants at M11): `xp.ts` — flat `XP_AWARDS` (join 10 < judge 30 < submit 50, the "shipping is the measured skill" ordering pinned by test), `winXp(placement, fieldSize)` (linear placement share of `WIN_XP_BASE` 200 — 1st gets full, last eligible gets base/field; monotonicity swept as an invariant), `streakXp`/`advanceStreak` (consecutive CLOSED pools completed; cancelled pools never call it so they can't break a streak), and `basePoolXp` which returns the POOL-LOCAL breakdown only — the streak bonus is deliberately excluded because it depends on prior profile state; `levels.ts` — triangular curve `xpForLevel(L)=100·(L-1)L/2` (L2=100, L3=300, L4=600…), `levelForXp` via a bounded loop not the float-fragile closed form, boundaries tested exactly; `rank.ts` — `poolRankPoints` PURELY ADDITIVE in v1 (you never lose rank → difficulty unlocks are a ratchet and "losses in aggregate only" holds for free; the can-go-down rating is battle Elo's job), harder pools weight more (beginner 1/intermediate 2/advanced 3). Also added `reconcileBallots` to the M3 vote-aggregation kernel (the drift case tally.ts flagged for M9): an anti-cheat flag landing AFTER a judge ranked the now-excluded entry would make `aggregateVotes` throw — reconcile strips non-judgeable entries from each ranking, drops ballots left < 2 long, AND drops ballots whose judge no longer owns a judgeable entry (their own build was flagged out → the kernel stops recognising them as an entrant-judge); judge-to-win eligibility is computed from the RAW ballots before reconciliation, so a judge whose ballot gets trimmed keeps their eligibility. Migration `0007`: `pool_results` (one row per entrant, unique (pool,user) = the idempotency lock) + `profiles.pool_streak`. `close-pool` slice (6 tests): the slice owns the pool-local derivation (who counts as submitted (`hasSubmission && isJudgeable` — a flagged entry keeps join XP but earns no submit XP and can't place), who judged, placement from `finalPlacements`, base XP + rank points), and hands a per-entrant award list to `deps.finalizeResults`; the streak bonus + new level are computed in close-deps UNDER A ROW LOCK (`FOR UPDATE` on the profile) because they read prior state and one user can finish two pools in the same tick — result-insert + profile-bump share a transaction (atomic) and the unique index gates the bump (idempotent: a re-run writes nothing, bumps nothing). Wired `finalize-results` into `tick-pools` (was recorded-pending at M5) — added a `finalizeResults` dep that runs `closePool(makeClosePoolDeps())`, the effect EXECUTES before the status persists (crash-safe), removed the now-dead `recordUnhandledEffect` (every PoolEffect now has an executor); cli.ts wires it, tick test asserts finalize-before-persist ordering. `results.ts` read model + `/pools/[poolId]/results` page + `PoolResultsBoard` (server-rendered podium 2nd·1st·3rd with the gold/silver/bronze plates, a "you earned" XP/rank/level/streak panel, full standings via `LeaderboardRow`; the reveal is a pure-CSS `reveal-up` stagger so content stays in the DOM JS-off and under reduced-motion); pool detail gains a "View results" CTA when closed; dashboard now shows REAL level/XP/rank/streak (the gamification finally visible — the product IS gamification). Full-loop e2e (`close.spec`): three entrants judge their anonymized set via the M8 UI, then the REAL lifecycle cron (`npm run pools:tick`, spawned by a `runPoolsTick` helper) closes the pool and finalizes — asserting the reveal podium, the per-entrant XP earned, the closed-pool "View results" route, and the dashboard reflecting non-zero XP end to end (join/build/submit are seeded + covered by pools.spec/submit.spec — one pool can't show every window in one run). Numbers verified live in the screenshot: 1st = +290 XP (90 base + winXp(1,3)=200) / +30 rank, "you" 3rd = +157 / Level 2 at 157 XP. 347 unit + 14 e2e green (serial; one transient operator.spec cold-compile flake passed on its own re-run — the known parallel flakiness, unrelated); screenshots `.claude/debug-shots/m9-*.png`. Commit `04c8520`.
- 2026-06-10 — **M8: Peer judging** — the front half of `judging → closed` (assign → rank → aggregate; the close/XP award is M9). New pure kernel `domain/prize-pools/judge-assignment` (test-first, 17 tests incl. fairness invariants swept over n=3..30): `assignJudges(entries, seed)` is a seeded Fisher-Yates shuffle into a ring + a **circulant** pairing (judge at ring-position i reviews the k entries that follow it, offsets 1..k mod n). Two guarantees fall straight out of the construction and are tested as INVARIANTS not examples — self-judging is impossible (offset 0 is never assigned; k ≤ n-1 so the window never wraps to you) and coverage is perfectly fair (every entry is the "next k" of exactly k judges → balanced in-degree == out-degree == k). Randomness (anti-gaming) is the shuffle; seeding from the pool id makes it DETERMINISTIC, which is the idempotency key. `reviewSetSize` caps at min(5, n-1) with a `MIN_JUDGEABLE_ENTRIES`=3 floor (a 2-entry pool can't form a comparative ballot — vote-aggregation's checkBallot rejects length-1). `checkAssignmentBallot` = the "complete your judging duty" gate (rank EXACTLY the assigned set — feeds judge-to-win eligibility). Migration `0006`: `judging_assignments` (one row per judge×entry, unique → idempotent upsert) + `ballots` (one ranking per judge per pool, unique → at-most-once; a row's existence IS "duty complete"). `assign-judges` slice (4 tests): `ensurePoolAssignments` loads the judgeable set (reuses M7's `listJudgeableEntries` — flagged entries are never assigned), runs the kernel, persists; idempotent via `hasAssignments` short-circuit + conflict-safe insert. Wired into `tick-pools`: the `assign-judges` lifecycle effect now EXECUTES (was recorded pending) — added an `assignJudges` dep, the CLI wires the real `makeAssignJudgesDeps`; updated the tick test. `cast-vote` slice (6 tests): load context → state guards (judging-open, assigned, at-most-once) → kernel `checkAssignmentBallot` (coverage) → kernel `checkBallot` (structural — self-vote impossible, defence-in-depth) → persist; no DB write until every gate passes. `tally` read (2 tests) = aggregation wiring: feeds real ballots through M3's `aggregateVotes`, deriving completion from "who cast a ballot" so a judge who skipped their duty is excluded from `finalPlacements` even when their own entry scored top (judge-to-win, proven). Judging UI: `judge-task` read model lazily ENSURES assignments on first judging-page view (same lazy-materialise pattern as `ensureProfile` — cron-timing independent) and serves each judge ONLY their assigned entries as anonymized "Submission A/B…" with the access-controlled playback URL (this IS the "only assigned judges can view a submission" rule, server-enforced); `/pools/[poolId]/judge` page + `JudgePanel` (drag-and-drop reorder with up/down a11y fallback, hidden `ranking` field re-validated server-side); pool-detail "Judge submissions" CTA during judging → "Judging complete" after. The `<video>` player is wired to the URL; in dev (no Cloudflare) it shows a clean placeholder — the real signed-Stream playback stays the M18/Needs item. multi-user e2e (`judging.spec`): three entrants each open the page, see their anonymized set (no entrant identity leaks — asserted), reorder, submit; round shows complete on re-entry (ballot persisted). 308 unit + 13 e2e green (serial; the suite's known parallel cold-compile flakiness is unrelated); screenshots `.claude/debug-shots/m8-*.png`. Commit `7aaa122`.
- 2026-06-10 — **M7: Pool anti-cheat** — the second authenticity layer over M6's fresh-repo anchor (pools ALLOW AI tools; anti-cheat polices AUTHENTICITY, not assistance). Two new pure kernel rules in `domain/prize-pools`, test-first: `assessOriginality` (16 tests) is the duplicate/reuse scoring predicate — it takes similarity SCORES (the comparing is infra's job) and flags `duplicate-co-entry` (another entrant's submission ≥ threshold — collusion/copying) and/or `reused-prior-work` (the entrant's OWN prior submission ≥ threshold — the fresh-repo-per-competition rule at the content level), inclusive thresholds (default 0.8, per-kind tunable), deterministic worst-first match ordering + dedup'd flags, throws on out-of-range scores; `moderation` (10 tests) owns the flag lifecycle `none→flagged→upheld|cleared` with `isJudgeable` (none/cleared in, flagged/upheld out — THE "excluded from judging/results pending review" rule), `canAutoFlag` (a scan only touches `none`, so a re-run never overturns the operator nor double-flags), and `reviewFlag` (only an open flag is reviewable, mirrors `approvePool`). New `infra/similarity` seam (mirrors infra/video + infra/github): `LocalSimilarityClient` = Jaccard over token sets (5 tests), `repoFingerprint` derives tokens from the normalized GitHub identity (owner+name) so two entries on the same repo score 1.0 — the blatant duplicate detectable without cloning; the real content-shingling client → Needs from Sampo. Migration `0005`: moderation columns on `entries` (`moderation_status` default 'none', `flag_reasons`/`flag_matches` jsonb, `flagged_at`/`reviewed_at`) — defaults make every pre-M7 row judgeable. `scan-submissions` slice (6 tests): a POST-HOC pass comparing each submitted entry against every OTHER entrant in the pool (different userId = collusion) and the entrant's own prior submissions, applying the kernel verdict and persisting flags — already-reviewed entries stay comparison TARGETS but aren't re-evaluated; shared `scan-deps` DB/similarity wiring drives both the operator action and the host-cron `npm run pools:scan` CLI (CLAUDE.md Commands updated). `review-flag` slice (8 tests) = the uphold/clear operator move (kernel-gated, operator re-checked in the action — it's a public endpoint). `/operator/flags` review queue (shared `OPERATOR_NAV`) with scan + uphold/clear; entrants see an "under review" / "removed from judging" notice on the pool page (`MySubmission.moderationStatus`). Structural exclusion wired for M8: `listJudgeableEntries` filters submitted-AND-not-excluded via the kernel's `JUDGING_EXCLUDED_STATUSES`, so judging is built from judgeable entries only — a flagged entry is never put in front of a judge. flagged-path e2e: two entrants submit the identical repo → operator scans → both flagged → clear one (false positive) leaves the queue, the other stays; uses `/dev/login` for a non-flaky operator session + new `seedUser`/`addSubmittedEntry` helpers. 278 unit + 12 e2e green; screenshots `.claude/debug-shots/m7-*.png`. Commit `fd239e5`.
- 2026-06-10 — **M6: Build window — fresh repo + submission** — two new pure kernel rules in `domain/prize-pools/submission` (test-first, 20 tests): `checkSubmissionWindow` is the deadline/state gate ("deadline enforced by the kernel" — must be `building`, before `buildDeadline` which is inclusive like `tickPool`, an entrant, not a dupe; collects ALL failed reasons like `checkJoin`), and `checkRepoFreshness` is the anti-cheat anchor over GitHub **server-side** signals only — repo created at/after the build window opened (the "prepared earlier" cheat) + ≥1 push inside `[openedAt, closesAt]` (inclusive bounds), never client commit timestamps. The build window is `[joinDeadline, buildDeadline]` (extension shifts both, so it stays correct) — no new column needed. `infra/github` extended: `fetchRepoSignals` returns a discriminated `RepoSignalsResult` so the slice tells `not-found` from `rate-limited` (the rate-limit awareness the real client maps from a 403 + `X-RateLimit-Remaining: 0`); pure `parseGitHubRepoUrl` (19 tests) accepts the forms students paste (https/ssh/shorthand/.git/trailing path) and rejects non-GitHub hosts; mock returns created-now/pushed-now signals (latest possible timestamp → always fresh for any `building` pool). New `infra/video` seam mirroring the github gate: `LocalVideoClient` writes demos to `.dev/videos/` (the dev fallback), real Cloudflare Stream client gated behind `CLOUDFLARE_*` → M18. Migration `0004`: nullable submission columns on `entries` (`repo_url`, `repo_created_at` (GitHub-side, audit/M7), `video_id`, `video_playback_url`, `submitted_at`). `submit-entry` slice (11 tests): orchestrates load → kernel window verdict → verify repo → kernel freshness verdict → store video → record, performing NO side effect until every gate passes (verification GATES linking — M6 is the hard check; soft duplicate/plagiarism flagging is M7); action parses the video `File` → `Buffer`, wires real infra, 200MB cap. `SubmitEntryForm` on the pool detail "You're in" card; `getPoolDetail` extended with `mySubmission` so the card shows the form (building), the submitted summary (repo link + timestamp), the "opens later" note (pre-building), or "closed" (post-building). E2E `submit.spec` (happy path + invalid-URL rejection) on a new `seedBuildingPool` + `addEntrant`; hardened the shared onboarding helper to wait for step 2 before connecting GitHub (kills a latent two-action race). 233 unit + 11 e2e green (e2e deterministic serially; `fullyParallel` flakes on dev-server cold-compile contention, not logic); screenshots `.claude/debug-shots/m6-*.png`. Commit `14876ea`.
- 2026-06-10 — **M5: Browse & join pools** — credit policy joined the kernel: `domain/prize-pools/credits` (STARTING_CREDITS 5, JOIN_CREDIT_COST 1, `creditDelta` as the single source of signed amounts) + `insufficient-credits` guard added to `checkJoin`. Migration `0003`: `credit_transactions` ledger (signed amounts, unique user+pool+reason = idempotency lock against double debit/refund); `profiles.credits` is the cached balance, the ledger is the audit trail; `infra/db/profiles.ensureProfile` materializes the profile + starter grant on first touch (PRD "grant on sign-up", realized lazily, race-safe via PK conflict). `join-pool` slice: kernel verdict up front, then transactional `recordJoin` re-checks the race-prone guards under a pool-row lock (`FOR UPDATE` serializes capacity; conditional `credits >= cost` UPDATE guards balance; entries unique guards dups) — stale-verdict races surface as ordinary rejections. `tick-pools` slice + `npm run pools:tick` (host cron command, CLAUDE.md Commands updated): kernel `tickPool` decides, slice executes; **effects run before the status persist** so a crash re-runs effects (refunds dedupe on the ledger) instead of stranding them; assign-judges/finalize-results recorded as pending (M8/M9); per-pool error isolation. `browse-pools` slice: `buildPoolView` computes the checkJoin verdict ONCE for listing+detail+button; `/pools` (role-filtered, difficulty filter chips, credits/rank header, My pools section) + `/pools/[poolId]` (brief/requirements/windows/entrants, JoinPoolButton via useActionState, rejection reasons listed); shared candidate reads in `infra/db/pool-queries` (the infra seam instead of cross-slice imports); `lib/nav` MAIN_NAV; dashboard wired to /pools (+ fixed pre-existing mojibake em-dashes). Verified live: published→extended (+48h all three deadlines, notify) → extended→cancelled (refund 4→5 credits, ledger `pool-join -1`/`pool-refund +1`, notify) → re-tick idempotent. 183 unit + 9 e2e green; screenshots `.claude/debug-shots/m5-*.png`. Commit `3beed2e`.
- 2026-06-10 — **M4: Pool persistence, manual import & approval queue** — migration `0002`: `profiles` (xp/level/globalRank/credits — M9 owns movement, M5 owns credit policy), `pools` (kernel-typed status/role/difficulty columns; window DURATIONS from the spec + nullable deadlines stamped at approval; `rejectedAt` = archival metadata, NOT a lifecycle state — rejected drafts keep status `draft`, leave every queue, and their slug stays claimed so re-import can't resurrect them), `entries` (unique pool+user). New kernel rules (test-first): `domain/identity/operator` (env-allowlist predicate, deny-by-default) + `domain/prize-pools/schedule` (`schedulePool` lays join/build/judging windows end-to-end from the publish instant — specs are written before approval time is known). `import-pools` slice: pure multi-entry frontmatter parser (`spec-format.ts`, collects ALL problems per entry, `---` is structural → briefs use `***` hr), orchestration with injected deps (file/batch/DB dedupe by slug, valid entries import even when siblings are malformed, exit 1 on any error), `tsx` CLI; format doc + 15 starter specs (3×5 roles) in `content/pools/`; verified live: 15 created, re-run 15 skipped. `approve-pool` slice: approve = kernel `approvePool` + `schedulePool` → publish with deadlines (verified in DB: +3d/+10d/+13d), reject = `rejectedAt` stamp; operator gate enforced in the server actions (not just the page — actions are public endpoints), non-operators 404. `getIdentity` moved `features/identity/session.ts` → `infra/auth/identity.ts` (slices in other features need it; cross-slice imports are the VSA smell). `OPERATOR_EMAILS` env (+ example). Found `.env` pointing at a dead Supabase URL — restored docker db, flagged in Needs. 155 unit + 7 e2e (operator gate ×3) green; screenshots `.claude/debug-shots/m4-*.png`. Commit `30bdbb2`.
- 2026-06-10 — **M3: Pool domain kernel** — `src/domain/prize-pools/` (pure, zero infra imports): `lifecycle.ts` — explicit transition table, operator `approvePool`, time-driven `tickPool(pool, now)` returning new snapshot + effects-as-data (`refund-credits`, `notify-extension`, `assign-judges`, `finalize-results`) for the M5 cron slice to execute; extension shifts all three deadlines +48h; deadlines inclusive (`now >= deadline` fires); defensive published→cancelled edge keeps the rule total. `entry.ts` — `checkJoin` collects ALL failed guards (role match, rank-gated difficulty tiers beginner/0 intermediate/100 advanced/250 (tunable until M9), cap-3 via shared `ACTIVE_POOL_STATUSES`, window/capacity/dup). `vote-aggregation.ts` — normalized Borda (`(k-1-i)/(k-1)`, mean per entry) so different ballot sizes compare; `checkBallot` makes self-votes impossible by construction; judge-to-win filter splits honest `standings` from awarded `finalPlacements`; deterministic ties (score → firsts → entryId) + order-independent fold; throws on corrupt ballots. 77 tests, written first. Commit `eb59509`.
- 2026-06-10 — **M2: Identity** — pure enrolment gate in `domain/identity/` (exact-domain `@sussex.ac.uk`, lowercasing, plus-tag stripping — 22 tests incl. lookalike domains), Auth.js v5 magic-link (custom EmailConfig through mockable `infra/email`; dev adapter logs + writes `.dev/outbox.jsonl` which the e2e reads as its inbox; `normalizeIdentifier` enforces the gate at the auth boundary), Drizzle adapter schema (users/accounts/sessions/verification_tokens + jobRole, migration `0001`), VSA slices `sign-in`/`select-role`/`connect-github` with injected-deps tests, mock GitHub connector behind `infra/github` seam, guarded `/` → `/onboarding` (2-step) → `/dashboard` via one `getIdentity()` loader + kernel `onboardingStatus`. 35 unit + 4 e2e green; screenshots in `.claude/debug-shots/m2-*.png`. Vitest now resolves the `@/` alias. Commit `91478d2`.
- 2026-06-10 — **M1: Design system** — "arena terminal" aesthetic: Tailwind v4 `@theme` tokens (volt `#bfff3f` accent, gold/silver/bronze/elo, OLED blue-black surfaces, cut-corner signature, snap easing), Russo One + Chakra Petch + JetBrains Mono via `next/font`, 9 primitives in `src/components/` (button/card/badge/input+field/modal/nav shell/page layout/leaderboard row+stat card/toast) with a11y wiring (focus-visible volt ring, aria-live toasts, labelled dialog, reduced-motion), `/styleguide` page + client demo island, screenshots (desktop 1440 / mobile 390 / modal open) in `.claude/debug-shots/`. 7 unit tests green. Commit `b6951b0`.
- 2026-06-10 — **M0: Scaffold & toolchain** — Next.js 15.5 (App Router, TS 5.9 strict), ESLint 9 + Prettier, VSA skeleton (`features/domain/infra/realtime/components/lib` + `content/pools` + `tests/e2e`), Drizzle + Postgres 17 (docker compose `db`, first migration applied), Vitest (4 tests) + Playwright smoke e2e passing, quality-gate + pre-commit hooks verified live. TS pinned to 5.x (TS 6 breaks Next 15 CSS imports). Old top-level `skills/` removed (superseded by `.claude/skills/`). Commit `84cabeb`.
