# Copy to .env for local dev. Values below match docker-compose.yml's db service.
DATABASE_URL=postgres://jrdev:jrdev@localhost:5432/jrdev

# Auth.js (M2) — generate with: npx auth secret (or any 32+ byte random string)
AUTH_SECRET=change-me-generate-a-real-one
